describe('signIn', () => {
  it ('deve retornar uma string de email', () => {
    expect().toEqual();
  });
});

const immediate = jest.requireActual('timers').setImmediate;
const flushPromises = () => new Promise(immediate)